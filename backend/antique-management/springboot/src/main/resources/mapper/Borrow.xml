<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springboot.mapper.BorrowMapper">


    <insert id="save">
        insert into borrow(item_name,item_no,user_address,user_name,user_phone,updatetime)
        values (#{itemName},#{itemNo},#{userAddress},#{userName},#{userPhone},#{updatetime})
    </insert>

    <update id="updateById">
        update borrow set item_name = #{itemName}, item_no = #{itemNo},user_address = #{userAddress}, user_name = #{userName},user_phone = #{userPhone},
                           updatetime = #{updatetime}  where id = #{id}
    </update>


    <delete id="deleteById">
        delete from borrow where id = #{id}
    </delete>

    <select id="list" resultType="com.example.springboot.entity.Borrow">
        select * from borrow order by id desc
    </select>

    <select id="listByCondition" resultType="com.example.springboot.entity.Borrow">
        select * from borrow
        <where>
            <if test="itemName != null and itemName != ''">
                item_name like concat('%', #{itemName}, '%')
            </if>
            <if test="itemNo != null and itemNo != ''">
                and item_no = #{itemNo}
            </if>
            <if test="userName != null and userName != ''">
                user_name like concat('%', #{userName}, '%')
            </if>
        </where>
    </select>

    <select id="getById" resultType="com.example.springboot.entity.Borrow">
        select * from borrow where id = #{id}
    </select>


    <select id="getByUsername" resultType="com.example.springboot.entity.Borrow">
        select * from borrow where user_name = #{userName}
    </select>




</mapper>